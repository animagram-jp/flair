[package]
name    = "flair"
version = "0.1.0"
edition = "2024"

[features]
default = ["std"]
std = []

[dependencies]
libm = "0.2"

[dev-dependencies]
nalgebra = "0.33"

[lib]
name = "flair"
path = "src/lib.rs"
